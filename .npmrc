# Exclude peer dependencies from security audits
# n8n-core and n8n-workflow are maintained by n8n team
audit-level=moderate

# Workspace isolation settings
# NOTE: This project uses a local pnpm-workspace.yaml file to create
# an independent workspace boundary, preventing pnpm from using the
# parent directory workspace
ignore-workspace=true
